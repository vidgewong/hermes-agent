## ADDED Requirements

### Requirement: Brand text displays as single-line mixed-case OpenStar
The system SHALL display the sidebar brand text as "OpenStar" on a single line, using the original mixed-case spelling (not uppercase).

#### Scenario: Sidebar brand renders correctly
- **WHEN** user views the dashboard sidebar
- **THEN** the brand text shows "OpenStar" in one line without forced uppercase
